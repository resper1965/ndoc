# Variáveis de Ambiente - n.doc
# Copie este arquivo para .env.local e preencha com seus valores

# ============================================
# SUPABASE (OBRIGATÓRIO)
# ============================================
# Obtenha essas credenciais em: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anonima_aqui

# ============================================
# UPSTASH REDIS (OBRIGATÓRIO EM PRODUÇÃO)
# ============================================
# ⚠️  IMPORTANTE: Em produção, Redis é OBRIGATÓRIO para rate limiting distribuído
# Em desenvolvimento, fallback em memória será usado se não configurado
# Obtenha em: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://seu-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=seu_token_redis_aqui

# ============================================
# NODE ENV (gerenciado automaticamente pela Vercel)
# ============================================
# Valores possíveis: development, production, test
# NODE_ENV=production (não precisa configurar manualmente)

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. NUNCA commite o arquivo .env.local
# 2. Todas as variáveis NEXT_PUBLIC_* são expostas no browser
# 3. Use Supabase Vault para armazenar API keys de IA (não use env vars)
# 4. Configure rate limiting (Redis) antes de ir para produção
